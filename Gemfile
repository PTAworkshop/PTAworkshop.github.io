source "https://rubygems.org"

# This mirrors what GitHub Pages uses in production, so `bundle exec jekyll serve`
# locally will behave the same as the live site. You only need this if you want
# to preview the site on your own computer before pushing to GitHub.
gem "github-pages", group: :jekyll_plugins
